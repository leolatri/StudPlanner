export interface BookDTO {
    id: string;
    name: string;
    autors: string[];
};

export interface ContactDTO {
    id: string;
    img: string;
    fio: string;
    email: string;
    uniSubjects: string[];
};