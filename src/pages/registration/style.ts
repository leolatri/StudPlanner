import { StyleSheet } from "react-native";

const st = StyleSheet.create({
    singIn: {
        width: '100%',
        height: '100%',

        justifyContent: 'center',
        alignItems: 'center',
    }
});

export default st;